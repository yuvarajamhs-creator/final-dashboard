# Copy to .env and fill in values. Do not commit .env.

# Meta (Facebook) - User Access Token used for me/accounts and to obtain Page tokens.
# Must include instagram_manage_insights and pages_read_engagement for Instagram Insights and Page Insights to work.
META_SYSTEM_ACCESS_TOKEN=
# Optional: use a different token for GET /api/meta/pages only (Content Marketing PAGE filter). When set, this token is used to fetch the page list; page tokens from this response are cached for insights.
META_SYSTEM_ACCESS_TOKEN_1=
# META_ACCESS_TOKEN=  # alternative name

# Wix Analytics (for Platform filter + Wix API integration)
# Site ID from your Wix dashboard URL (e.g. manage.wix.com/.../95f10d80-dbd9-4d57-a4f7-d84de82d5b88/...)
# Token is instance/IST token from Wix with "Site Analytics - read" permission.
WIX_SITE_ID=
WIX_TOKEN=
